#include "main.h"
/**
 * _abs - function to return the absoluten.
 *
 * @n : The character to be checked.
 *
 * Return: Return the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
