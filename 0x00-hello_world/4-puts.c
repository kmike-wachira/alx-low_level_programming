#include <stdio.h>
#include <string.h>
/*
 *main - function
 *Description :  'print using puts'
 * Returns 0
 */
int main(void)
{
	char str[100];

	strcpy(str, "\"Programming is like building a multilingual puzzle");
	puts(str);
	return (0);
}

