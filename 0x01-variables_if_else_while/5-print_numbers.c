#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - Entry point
 *
 *Return: always 0 (Success)
 */
int main(void)
{
	int no;

	for (no = 0; no < 10 ; no++)
	{
		printf("%d", no);
	}
	printf("\n");
	return (0);
}
